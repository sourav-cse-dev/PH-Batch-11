function tenTimes(number) {
  const result = number * 10;
  return result;
}
const result = tenTimes(5);
console.log(result);

function cutHalf(number) {
  const half = number / 2;
  return half;
}
const value = cutHalf(10);
console.log(value);
