//New element can be added to an array using push() method
const age = []; // we can declare empty array
const numbers = [12, 87, 98, 45];
console.log(numbers);

// we can push a single value in a array
numbers.push(44);
console.log(numbers);

// we can push multiple values in a array
numbers.push(98, 11, 9);
console.log(numbers);
