//join() method returns an array as a string with comma separator by default, we can define separator
const fruits = ["Banana", "Orange", "Apple", "Mango"];
let text = fruits.join("+");

console.log(text);
