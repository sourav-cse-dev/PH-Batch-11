//Element can be removed using shift() method from first index
const numbers = [12, 87, 98, 45];
console.log(numbers);

// we can remove first value in a array using shift
numbers.shift();
console.log(numbers);
